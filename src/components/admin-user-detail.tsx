"use client";

import Link from "next/link";
import { useMemo, useState, useTransition } from "react";
import { useRouter } from "next/navigation";
import { Activity, AlertTriangle, BookOpen, CheckCircle2, Clock3, KeyRound, Library, RefreshCw, Shield, Users } from "lucide-react";

type LibraryItem = { id: string; title: string; slug: string; status: string; syncStatus: string; syncStartedAt: string | null; syncError: string | null; retryable: boolean; lastReadAt: string | null; lastReadChapterNumber: number | null; latestChapterNumber: number | null; unreadChapters: number; preferredSource: string | null; sourceFailureCount: number; sourceError: string | null; accessReason: string };
type ActivityItem = { label: string; detail: string; at: string | null };
type Job = { id: string; mangaTitle: string; status: string; error: string | null; createdAt: string; userAttributed: boolean };
type FamilyLink = { id: string; label: string; accountId: string | null; name: string; email: string; status: string };

export type AdminUserDetailData = {
  id: string; actorId: string; name: string; email: string; role: "USER" | "ADMIN"; providers: string[]; emailVerified: string | null; familyRole: string;
  health: "healthy" | "attention"; issues: string[]; activeSessions: number; library: LibraryItem[];
  lastReadAt: string | null; lastTrackedAt: string | null; lastChatAt: string | null; recentActivity: ActivityItem[]; jobs: Job[];
  familyLinks: FamilyLink[]; policy: { enabled: boolean; blockedTagNames: string[]; allowedContentRatings: string[] } | null; overrideCount: number;
};

const dateFormatter = new Intl.DateTimeFormat("en", { dateStyle: "medium", timeStyle: "short" });
const formatDate = (value: string | null) => value ? dateFormatter.format(new Date(value)) : "Never";

export function AdminUserDetail({ data }: { data: AdminUserDetailData }) {
  const router = useRouter();
  const [tab, setTab] = useState<"library" | "activity" | "family">("library");
  const [query, setQuery] = useState(""); const [statusFilter, setStatusFilter] = useState("ALL"); const [syncFilter, setSyncFilter] = useState("ALL");
  const [message, setMessage] = useState(""); const [pending, startTransition] = useTransition();
  const unread = data.library.reduce((sum, item) => sum + item.unreadChapters, 0);
  const retryable = useMemo(() => data.library.filter((item) => item.retryable), [data.library]);
  const library = useMemo(() => data.library.filter((item) => item.title.toLowerCase().includes(query.toLowerCase()) && (statusFilter === "ALL" || item.status === statusFilter) && (syncFilter === "ALL" || item.syncStatus === syncFilter)).sort((a, b) => Number(retryable.some((item) => item.id === b.id)) - Number(retryable.some((item) => item.id === a.id))), [data.library, query, retryable, statusFilter, syncFilter]);

  const act = (request: () => Promise<Response>, success: (body: Record<string, unknown>) => string) => startTransition(async () => {
    setMessage(""); const response = await request(); const body = await response.json().catch(() => ({}));
    if (!response.ok) { setMessage(String(body.error ?? "Action failed")); return; }
    setMessage(success(body)); router.refresh();
  });

  const retry = (ids?: string[]) => act(() => fetch(`/api/admin/users/${data.id}/sync-retries`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(ids ? { userMangaIds: ids } : {}) }), (body) => `${body.queued ?? 0} synchronization${body.queued === 1 ? "" : "s"} queued.`);
  const changeRole = () => { const next = data.role === "ADMIN" ? "USER" : "ADMIN"; if (!confirm(`Change ${data.name}'s role to ${next.toLowerCase()}?`)) return; act(() => fetch(`/api/admin/users/${data.id}`, { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ role: next }) }), () => "Role updated."); };
  const revokeSessions = () => { if (!confirm(`Sign ${data.name} out of every active session?`)) return; act(() => fetch(`/api/admin/users/${data.id}/sessions`, { method: "DELETE" }), (body) => `${body.revoked ?? 0} session${body.revoked === 1 ? "" : "s"} revoked.`); };
  const unlink = (link: FamilyLink) => { if (!confirm(`Unlink ${link.name}? The child policy and title overrides for this relationship will be removed.`)) return; act(() => fetch(`/api/admin/users/${data.id}/family-links/${link.id}`, { method: "DELETE" }), () => "Family relationship removed."); };

  return <>
    <section className="surface mt-5 rounded-lg p-5 sm:p-6">
      <div className="flex flex-col gap-5 lg:flex-row lg:items-start lg:justify-between"><div><div className="flex items-center gap-3"><div className="flex h-12 w-12 items-center justify-center rounded-full bg-primary/10 text-primary"><Users className="h-6 w-6" /></div><div><h1 className="text-2xl font-bold sm:text-3xl">{data.name}</h1><p className="text-sm text-muted-foreground">{data.email}</p></div></div><div className="mt-4 flex flex-wrap gap-2"><Badge>{data.role === "ADMIN" ? "Admin" : "User"}</Badge><Badge>{data.familyRole}</Badge>{data.providers.map((provider) => <Badge key={provider}>{provider}</Badge>)}</div></div>
        <div className={`rounded-lg border p-4 lg:w-80 ${data.health === "attention" ? "border-amber-500/40 bg-amber-500/10" : "border-emerald-500/40 bg-emerald-500/10"}`}><div className="flex items-center gap-2 font-semibold">{data.health === "attention" ? <AlertTriangle className="h-4 w-4 text-amber-500" /> : <CheckCircle2 className="h-4 w-4 text-emerald-500" />}{data.health === "attention" ? "Needs attention" : "Account healthy"}</div>{data.issues.length ? <ul className="mt-2 list-disc pl-5 text-sm">{data.issues.map((issue) => <li key={issue}>{issue}</li>)}</ul> : <p className="mt-2 text-sm text-muted-foreground">No actionable account issues detected.</p>}</div></div>
      <dl className="mt-6 grid gap-4 border-t border-border pt-5 text-sm sm:grid-cols-4"><div><dt className="text-muted-foreground">Email verified</dt><dd className="mt-1 font-medium">{formatDate(data.emailVerified)}</dd></div><div><dt className="text-muted-foreground">Last chapter read</dt><dd className="mt-1 font-medium">{formatDate(data.lastReadAt)}</dd></div><div><dt className="text-muted-foreground">Last title tracked</dt><dd className="mt-1 font-medium">{formatDate(data.lastTrackedAt)}</dd></div><div><dt className="text-muted-foreground">Last chat message</dt><dd className="mt-1 font-medium">{formatDate(data.lastChatAt)}</dd></div></dl>
    </section>
    <section aria-label="Account totals" className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-5">{[
      ["Health issues", data.issues.length, data.health === "attention" ? AlertTriangle : CheckCircle2], ["Library titles", data.library.length, Library], ["Unread chapters", unread, BookOpen], ["Active sessions", data.activeSessions, KeyRound], ["Retryable syncs", retryable.length, RefreshCw],
    ].map(([label, value, Icon]) => { const C = Icon as typeof Library; return <article key={String(label)} className="surface rounded-lg p-5"><div className="flex items-center justify-between text-sm text-muted-foreground"><span>{String(label)}</span><C className="h-4 w-4" /></div><p className="mt-3 text-3xl font-bold tabular-nums">{String(value)}</p></article>; })}</section>
    <div className="mt-6 flex flex-wrap gap-2 border-b border-border">{([['library','Library'],['activity','Activity & sync'],['family','Family & access']] as const).map(([id,label]) => <button key={id} className={`px-4 py-3 text-sm font-semibold ${tab === id ? "border-b-2 border-primary text-primary" : "text-muted-foreground"}`} onClick={() => setTab(id)}>{label}</button>)}</div>
    {message && <p role="status" className="mt-4 rounded-md bg-muted p-3 text-sm">{message}</p>}
    {tab === "library" && <section className="surface mt-4 overflow-hidden rounded-lg"><div className="border-b border-border p-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><h2 className="text-lg font-semibold">Library diagnostics</h2><p className="text-sm text-muted-foreground">Progress, access, source health, and synchronization state.</p></div>{retryable.length > 0 && <button disabled={pending} onClick={() => retry()} className="ui-button ui-button-primary"><RefreshCw className="h-4 w-4" /> Retry all affected</button>}</div><div className="mt-4 grid gap-2 sm:grid-cols-[1fr_170px_180px]"><input className="ui-field" aria-label="Search library" placeholder="Search titles" value={query} onChange={(event) => setQuery(event.target.value)} /><select className="ui-field" aria-label="Filter reading status" value={statusFilter} onChange={(event) => setStatusFilter(event.target.value)}><option value="ALL">All reading states</option>{[...new Set(data.library.map((item) => item.status))].map((status) => <option key={status} value={status}>{status.toLowerCase()}</option>)}</select><select className="ui-field" aria-label="Filter sync state" value={syncFilter} onChange={(event) => setSyncFilter(event.target.value)}><option value="ALL">All sync states</option><option value="FAILED">Failed</option><option value="SYNCING">Syncing</option><option value="UPDATED">Updated</option></select></div></div><div className="overflow-x-auto"><table className="w-full min-w-[980px] text-left text-sm"><thead className="bg-muted/50 text-xs uppercase text-muted-foreground"><tr><th className="px-5 py-3">Title</th><th className="px-5 py-3">Progress</th><th className="px-5 py-3">Access</th><th className="px-5 py-3">Source</th><th className="px-5 py-3">Sync</th><th className="px-5 py-3">Last read</th><th className="px-5 py-3"></th></tr></thead><tbody className="divide-y divide-border">{library.map((item) => { const canRetry = retryable.some((candidate) => candidate.id === item.id); return <tr key={item.id}><td className="px-5 py-4"><Link href={`/manga/${item.slug}`} className="font-medium hover:underline">{item.title}</Link><p className="text-xs capitalize text-muted-foreground">{item.status.toLowerCase()}</p></td><td className="px-5 py-4">{item.lastReadChapterNumber ?? "—"} / {item.latestChapterNumber ?? "—"}<p className="text-xs text-muted-foreground">{item.unreadChapters} unread</p></td><td className="px-5 py-4 capitalize">{item.accessReason.replaceAll("_", " ")}</td><td className="px-5 py-4">{item.preferredSource || "Automatic"}{item.sourceFailureCount > 0 && <p title={item.sourceError || undefined} className="text-xs text-amber-500">{item.sourceFailureCount} source failures</p>}</td><td className="px-5 py-4"><span title={item.syncError || undefined} className={canRetry ? "font-semibold text-amber-500" : ""}>{item.syncStatus.toLowerCase()}</span></td><td className="px-5 py-4 text-muted-foreground">{formatDate(item.lastReadAt)}</td><td className="px-5 py-4">{canRetry && <button disabled={pending} className="ui-button ui-button-secondary" onClick={() => retry([item.id])}>Retry</button>}</td></tr>; })}</tbody></table>{!library.length && <p className="p-8 text-center text-sm text-muted-foreground">No titles match these filters.</p>}</div></section>}
    {tab === "activity" && <div className="mt-4 grid gap-4 lg:grid-cols-2"><section className="surface rounded-lg p-5"><h2 className="flex items-center gap-2 text-lg font-semibold"><Activity className="h-5 w-5" /> Explicit activity</h2><div className="mt-4 space-y-4">{data.recentActivity.map((item) => <div key={`${item.label}-${item.at}`} className="border-l-2 border-border pl-4"><p className="font-medium">{item.label}</p><p className="text-sm text-muted-foreground">{item.detail}</p><p className="mt-1 text-xs text-muted-foreground">{formatDate(item.at)}</p></div>)}{!data.recentActivity.length && <p className="text-sm text-muted-foreground">No explicit activity recorded.</p>}</div></section><section className="surface rounded-lg p-5"><h2 className="flex items-center gap-2 text-lg font-semibold"><Clock3 className="h-5 w-5" /> Relevant sync jobs</h2><div className="mt-4 space-y-3">{data.jobs.map((job) => <div key={job.id} className="rounded-md border border-border p-3"><div className="flex justify-between gap-3"><span className="font-medium">{job.mangaTitle}</span><Badge>{job.status.toLowerCase()}</Badge></div><p className="mt-1 text-xs text-muted-foreground">{job.userAttributed ? "User-attributed" : "Shared manga job"} · {formatDate(job.createdAt)}</p>{job.error && <p className="mt-2 text-sm text-red-500">{job.error}</p>}</div>)}{!data.jobs.length && <p className="text-sm text-muted-foreground">No relevant jobs recorded.</p>}</div></section></div>}
    {tab === "family" && <div className="mt-4 grid gap-4 lg:grid-cols-2"><section className="surface rounded-lg p-5"><h2 className="flex items-center gap-2 text-lg font-semibold"><Shield className="h-5 w-5" /> Family & access</h2>{data.policy ? <dl className="mt-4 space-y-3 text-sm"><div><dt className="text-muted-foreground">Policy</dt><dd className="font-medium">{data.policy.enabled ? "Enabled" : "Disabled"}</dd></div><div><dt className="text-muted-foreground">Allowed ratings</dt><dd>{data.policy.allowedContentRatings.join(", ")}</dd></div><div><dt className="text-muted-foreground">Blocked tags</dt><dd>{data.policy.blockedTagNames.join(", ") || "None"}</dd></div><div><dt className="text-muted-foreground">Title overrides</dt><dd>{data.overrideCount}</dd></div></dl> : <p className="mt-3 text-sm text-muted-foreground">No child policy applies to this account.</p>}<div className="mt-5 space-y-3">{data.familyLinks.map((link) => <div key={link.id} className="rounded-md border border-border p-3"><Link href={link.accountId ? `/admin/users/${link.accountId}` : "#"} className="font-medium hover:underline">{link.label}: {link.name}</Link><p className="text-xs text-muted-foreground">{link.email} · {link.status.toLowerCase()}</p><button disabled={pending} onClick={() => unlink(link)} className="mt-3 text-xs font-semibold text-red-500 hover:underline">Unlink relationship</button></div>)}{!data.familyLinks.length && <p className="text-sm text-muted-foreground">No family relationships.</p>}</div></section><section className="surface rounded-lg p-5"><h2 className="text-lg font-semibold">Account controls</h2><p className="mt-1 text-sm text-muted-foreground">Sensitive changes are confirmed and protected against self-lockout.</p><div className="mt-5 space-y-3"><button disabled={pending || data.id === data.actorId} onClick={changeRole} className="ui-button ui-button-secondary w-full justify-center">Change role to {data.role === "ADMIN" ? "user" : "admin"}</button><button disabled={pending || data.id === data.actorId} onClick={revokeSessions} className="ui-button w-full justify-center border-red-500 text-red-500">Revoke all sessions</button>{data.id === data.actorId && <p className="text-xs text-muted-foreground">Self-demotion and self-session revocation are disabled to prevent lockout.</p>}</div></section></div>}
  </>;
}

function Badge({ children }: { children: React.ReactNode }) { return <span className="rounded-full bg-muted px-2 py-1 text-xs font-semibold capitalize text-muted-foreground">{children}</span>; }
